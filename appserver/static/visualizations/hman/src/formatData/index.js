// Optionally implement to format data returned from search. 
// The returned object will be passed to updateView as 'data'
const _formatData = function (data) {
  // Format data
  return data;
}

module.exports = _formatData;